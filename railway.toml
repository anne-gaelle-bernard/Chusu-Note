[build]
builder = "NIXPACKS"
buildCommand = "echo 'Skipping frontend build for backend deployment'"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
